package io.jpostman.schema.model;

/**
 * Defines the supported request body content types in the common API schema
 * model.
 */
public enum ApiBodyType {
	NONE, JSON, FORM_DATA, X_WWW_FORM_URLENCODED, RAW, GRAPHQL
}
